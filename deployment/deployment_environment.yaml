name: vint_deployment
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - torchvision
  - numpy
  - matplotlib
  - pyyaml
  - rospkg
  - pip:
      - torch
      - torchvision
      - efficientnet_pytorch
      - warmup_scheduler
      - diffusers==0.11.1
